#Code to open up your laptop Camera and close it using “q”
import cv2

cam = cv2.VideoCapture(0)  #0 :default camera, we can use the IP address of a diff. camera

while True:
    check, frame = cam.read()

    cv2.imshow('video', frame)

    key = cv2.waitKey(1)
    if key == ord("q"):
        break

cam.release()
cv2.destroyAllWindows()
